# ── Test environment overrides ──
# Copy this to .env.test and fill in the values.
# Any variable not set here falls back to your .env file.

# A separate bot token is recommended so test + production can run simultaneously.
# Create a second Application at https://discord.com/developers/applications
DISCORD_TOKEN=

# Your test/dev Discord server ID
TEST_GUILD_ID=

# Override log level for more verbose output during development
LOG_LEVEL=DEBUG

# You can point to a separate Firestore project for test data, or
# reuse the same one (data is scoped per guild_id in documents).
# FIREBASE_ENABLED=true
# FIREBASE_CREDENTIALS_PATH=
# FIREBASE_PROJECT_ID=
