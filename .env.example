# Required
DISCORD_TOKEN=

# Bot environment: "production" (default) or "test"
# When set to "test", the bot loads .env.test first, then .env as fallback.
# See .env.test.example for the test-specific overrides.
# BOT_ENV=production

# Optional (recommended for fast slash command sync in dev)
# GUILD_ID=123456789012345678

# Test/dev Discord server ID (used when BOT_ENV=test)
# TEST_GUILD_ID=123456789012345678

# Optional
# COMMAND_PREFIX=!

# Albion Online settings
# Market data host (West/Americas by default)
# ALBION_DATA_BASE=https://west.albion-online-data.com

# Official kill/battle API base
# ALBION_GAMEINFO_BASE=https://gameinfo.albiononline.com/api/gameinfo

# Backend / storage (Firebase / Firestore)
# Set to true once you have credentials configured.
# FIREBASE_ENABLED=false

# Path to a Firebase Admin SDK service account JSON file.
# Create one in Firebase Console: Project settings -> Service accounts.
# FIREBASE_CREDENTIALS_PATH=C:\\path\\to\\serviceAccountKey.json

# Optional: override project id (usually already in the JSON)
# FIREBASE_PROJECT_ID=
